import { selectImg } from '../../../helpers/select-img';
import { module, test } from 'qunit';

module('Unit | Helper | select img');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = selectImg(42);
  assert.ok(result);
});
